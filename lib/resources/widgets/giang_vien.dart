import 'package:app_qlsv/resources/strings.dart';
import 'package:flutter/material.dart';
class giangvien extends StatelessWidget {
  const giangvien({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: const Text(Giang_Vien),
        )
    );
  }
}

